<?php
# Using the define function with the first parameter as the name of the constant and the second parameter as the value of the constant,
# Values of these constants can either be strings, int, or boolean, arrays are also supported in php version >= 7.1
define("NAME", "James");

echo NAME;